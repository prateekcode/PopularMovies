# PopularMovies
In this project, I had build an app to allow users to discover the most popular movies playing.

To run the project successfully, you need to request an API key from https://www.themoviedb.org/.
Enter the API Key in strings.xml file
